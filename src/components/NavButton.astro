---
const { elSelector, name, Icon } = Astro.props;
---

<a href={elSelector}>
    <div class="tooltip tooltip-left" data-tip={name}>
        <button
            class="btn btn-xl btn-circle hover:border-primary hover:border-3"
        >
            <Icon />
        </button>
    </div>
</a>
